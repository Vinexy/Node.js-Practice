const circleCircumference = (radius) => {
  const circumference = parseInt(radius) * 2 * 3.14;
  console.log("Circle circleCircumference is: " + circumference.toFixed(2));
};

module.exports = { circleCircumference };
